#include <iostream>
#include <string>
using namespace std;

int main() {
    string c;
    float a, b;

    cin >> c >> a >> b;
    cout << fixed;
    cout.precision(2);

    cout << c << '\n' << a << '\n' << b ; 
    return 0;
}